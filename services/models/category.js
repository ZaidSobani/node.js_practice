class Category {
    constructor(id, CategoryName, createdDate, updatedDate) {
        this.id = id
        this.CategoryName = CategoryName
        this.createdDate = createdDate
        this.updatedDate = updatedDate
    }
}

module.exports = Category